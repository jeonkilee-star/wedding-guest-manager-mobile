# 결혼식 하객 관리 웹앱 개발 보고서

**작성일** 2026-09-09
**예식일** 2026-09-19 (신랑 이정민 · 신부 김예미)
**배포 주소** https://jeonkilee-star.github.io/wedding-guest-manager-mobile/
**저장소** jeonkilee-star/wedding-guest-manager-mobile

---

## 1. 목적

결혼식 하객 인원을 가족이 각자 휴대폰으로 입력하면 **실시간으로 합산**되는 도구.
별도 앱 설치나 로그인 없이 링크만으로 사용한다.

## 2. 최종 사양

| 항목 | 내용 |
|---|---|
| 형태 | 단일 `index.html` (프레임워크 없음) |
| 호스팅 | GitHub Pages |
| 저장소 | Firebase Cloud Firestore (`wedding-guests-21e70`, 서울 리전) |
| 인증 | 없음 — 링크를 아는 사람은 누구나 조회·입력 가능 |
| 기기 식별 | 최초 1회 역할 선택 후 `localStorage`에 저장, 이후 자동 진입 |
| 동기화 | Firestore `onSnapshot` 실시간 구독 (새로고침 불필요) |

### 화면 구성

1. **최초 진입** — 신랑측/신부측 선택 → 역할 선택 → 등록
2. **메인 화면** — 메모·인원수 입력 / 총 인원·신랑측·신부측 현황 / 역할별 현황 / 내 제출 내역
3. **설정** — 역할 변경

### 역할 목록

- 신랑측: 신랑 (이정민), 아버지, 어머니, 동생
- 신부측: 신부 (김예미), 아버지, 어머니, 언니

---

## 3. 설계가 바뀐 지점

개발 중 방향을 세 번 바꿨다. 각각의 이유를 남긴다.

### 3-1. 비밀번호 → 기기 등록 → 인증 제거

처음에는 신랑/신부만 통계를 볼 수 있도록 비밀번호를 걸었다. 그러나
**통계 화면에서 나갔다 들어올 때마다 비밀번호를 다시 요구**하는 문제가 반복됐고,
가족이 쓰기에 번거롭다는 판단으로 두 단계에 걸쳐 걷어냈다.

1. **기기 등록 방식 도입** — 폰마다 최초 1회 역할을 고르면 그 사람으로 고정
2. **비밀번호 완전 제거** — 누구나 전체 현황을 볼 수 있도록 개방

> 인증이 없는 것은 누락이 아니라 **의도된 선택**이다.
> 대신 링크는 가족에게만 공유해야 한다.

### 3-2. 별도 대시보드 → 메인 화면 통합

신랑/신부용 대시보드를 따로 두었으나, 필요한 정보(총 인원·양측 인원·역할별 내역)를
메인 화면에 모두 올릴 수 있어 화면 자체를 없앴다. 이동 단계가 하나 줄었다.

### 3-3. localStorage → Firebase Firestore

**가장 중요한 수정이었다.** 초기 구현은 Claude Artifact 전용 API(`claude.use("db")`)를
사용했는데, 이 API는 GitHub Pages에서 항상 실패하고 `localStorage`로 폴백한다.
즉 **폰끼리 데이터가 전혀 공유되지 않는 상태**로, 아버지 폰에 15명을 입력해도
신랑 폰에는 0명으로 보였다.

Firestore로 교체해 실제 공유를 구현했다. 검증 결과 기기 A의 입력이
기기 B에 **새로고침 없이 즉시** 반영되는 것을 확인했다.

---

## 4. 해결한 주요 문제

| 증상 | 원인 | 조치 |
|---|---|---|
| 화면이 검은색만 나옴 | 다크모드 CSS + `claude` 객체 부재로 JS 중단 | 라이트 모드 고정, `typeof claude` 가드 추가 |
| 비밀번호 설정 후에도 계속 재입력 요구 | 저장/조회 형식 불일치 | (이후 기능 자체를 제거) |
| 통계가 0으로만 표시 | `loadSubmissions()`가 비동기인데 `await` 누락 | `showMainApp()`을 async로 전환 |
| 삭제해도 데이터가 남음 | DB 사용 시 localStorage 미갱신 | 양쪽 모두 갱신하도록 수정 |
| 수정사항이 반영 안 됨 | GitHub Pages 기본 캐시 | 캐시 방지 메타 태그 추가 |
| 역할별 합계 ≠ 상단 총계 | 미리 정의된 역할만 집계 | 정의 외 역할도 표시하도록 수정 |

---

## 5. 보안 점검 결과

배포 후 실제 앱을 조작하며 점검했다.

### 5-1. XSS 취약점 (수정 완료)

메모 입력값을 `innerHTML`에 그대로 넣고 있었다. 누구나 쓸 수 있는 공유 DB이므로,
메모 칸에 `<img src=x onerror="...">` 를 넣으면 **그 스크립트가 가족 전원의 화면에서
실행**되는 상태였다. 실제 주입 테스트로 재현을 확인했다.

→ 출력 이스케이프 처리 후 재테스트. 스크립트가 실행되지 않고 글자 그대로 표시됨을 확인.

### 5-2. 전체 삭제 버튼 제거

설정에 있던 "모든 데이터 초기화"는 링크만 알면 누구나 가족 전체의 입력을
한 번에 지울 수 있었고 복구 수단이 없었다. **기능 자체를 삭제**했다.

### 5-3. Firestore 보안 규칙

`submissions` 컬렉션에 한해 **2026-10-31까지만** 읽기·쓰기를 허용한다.
다른 경로는 모두 차단된다.

```
match /submissions/{docId} {
  allow read: if request.time < timestamp.date(2026, 10, 31);
  allow create, update, delete: if request.time < timestamp.date(2026, 10, 31);
}
```

---

## 6. 오입력 방지

신부측에 450명이 한 번에 등록된 건을 계기로 점검했다.
확인 결과 **실제 인원이 맞아 데이터는 유지**했고, 예방책만 추가했다.

- 인원수 칸을 누르면 기존 값이 전체 선택된다 (기본값 `1` 뒤에 숫자가 붙는 것을 방지)
- 100명 이상 입력 시 확인창을 띄운다
- 저장 중에는 "추가하기" 버튼이 잠긴다 (연타로 인한 중복 등록 방지)

---

## 7. 현재 데이터

2026-09-09 기준

| 구분 | 인원 |
|---|---|
| 신랑측 | 20 |
| 신부측 | 450 |
| **합계** | **470** |

---

## 8. 남은 과제

우선순위 순.

1. **같은 역할끼리 서로의 기록을 삭제할 수 있다**
   "신랑측 아버지"로 등록한 폰이 두 대면 양쪽 모두 같은 목록을 보고 삭제할 수 있다.
   기기별 고유 ID를 부여하면 해결된다.

2. **수정 기능이 없다**
   숫자를 바꾸려면 삭제 후 재입력해야 한다.

3. **삭제에 되돌리기가 없다**
   예식 전날 데이터 백업을 권장한다.

4. **100명 확인창이 신부측에 걸리적거릴 수 있다**
   450 같은 큰 숫자가 정상 입력이라면 기준값 상향을 검토한다.

5. **보안 규칙 만료 (2026-10-31)**
   만료 후 앱은 조용히 로컬 저장으로 폴백한다. 이 시점 이후 계속 쓰려면 규칙을 연장해야 한다.

---

## 9. 개발 이력

2026-09-09 하루 동안 40개 커밋으로 진행했다. 주요 분기점만 추린다.

| 시각 | 커밋 | 내용 |
|---|---|---|
| 09:49 | `9e72762` | 최초 구현 |
| 10:33 | `2147166` | 역할별 입력 + 대시보드 구조 도입 |
| 11:50 | `ccf9a3c` | 기기 등록 방식으로 재설계 |
| 12:12 | `c64d0db` | `claude.use` 오류로 화면이 안 뜨던 문제 해결 |
| 13:58 | `63741b0` | 신랑/신부 비밀번호 시스템 |
| 14:59 | `da0e6b8` | 비밀번호 제거, 전체 공개로 전환 |
| 15:15 | `b828875` | Firebase Firestore 연동 |
| 16:35 | `22f1e6e` | Firebase 설정 반영, 실시간 동기화 활성화 |
| 17:33 | `e12a6ac` | XSS 수정 및 오입력 방지 |
| 18:02 | `774c538` | 전체 삭제 기능 제거 |
| 18:36 | `f683e02` | UI 마무리 |
